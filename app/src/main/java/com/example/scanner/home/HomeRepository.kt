package com.example.scanner.home

import com.example.scanner.data.SessionData
import javax.inject.Inject

class HomeRepository @Inject constructor(){
    fun postSessionData(sessionData: SessionData) {

    }
}